#include "main.h"
/**
 * _islower - Entry pointi
 * @c: print letter
 * Return:  1 (Lower), 0 (Upper)
 */

void more_numbers(void)
{
	int i;
	int n;

	for (i = 0 ; i <= 10 ; i++)
	{
		for (n = 0 ; n <= 14 ; n++)
		{
			_putchar(n + '0');
		}
		_putchar('\n');
	}
}
