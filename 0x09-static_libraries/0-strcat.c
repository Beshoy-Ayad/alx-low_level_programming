#include "main.h"
#include <stdio.h>
/**
 * _strncat - check
 * @dest : char
 * @src : char
 * @n : int
 * Return: always 0
 */

char *_strncat(char *dest, char *src, int n)
{
	if (*dest || *src)
	{
		if (n)
		{
			printf("n");
		}
	}
	return (0);
}
